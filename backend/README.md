# Backend

Flask app and site content. Run from **project root**:

```bash
pip install -r backend/requirements.txt
python backend/app.py
```

- **app.py** — Routes for `/`, `/research`, `/blogs`. Loads templates and static files from `frontend/`.
- **content.py** — Edit this to update all site text and data.
